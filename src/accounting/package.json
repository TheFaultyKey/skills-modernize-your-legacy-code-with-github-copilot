{
  "name": "accounting-app",
  "version": "1.0.0",
  "private": true,
  "description": "Single-file Node.js version of the legacy COBOL student account application.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest --runInBand"
  },
  "devDependencies": {
    "jest": "^30.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}